import 'package:bts_plus/domains/rabbit_card.dart';
import 'package:bts_plus/domains/user.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

// final rabbitProvider = StateNotifierProvider<RabbitCardNotifier, RabbitCard?>(
//   (ref) => RabbitCardNotifier(null),
//   // (ref) => RabbitCardNotifier(User.mockUp()),
// );

// class RabbitCardNotifier extends StateNotifier<RabbitCard?> {
//   RabbitCardNotifier(RabbitCard? state) : super(state);

//   setCurrentUser(User? user) {
//     state = user;
//   }

//   void clearUser() {
//     state = null;
//   }
// }
