import 'package:flutter/material.dart';

class PrimaryDropDown extends StatelessWidget {
  const PrimaryDropDown({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Dropdown');
  }
}
